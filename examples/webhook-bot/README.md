# Webhook Bot Example

A bot using webhooks to receive updates.

## When to Use

- Production deployments
- Serverless platforms (Vercel, Render)
- High-traffic bots

## How It Works

Telegram sends updates directly to your server via HTTP POST requests.

