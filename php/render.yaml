# Render Blueprint
services:
  - type: web
    name: telegram-bot-php
    env: docker
    dockerfilePath: ./php/Dockerfile
    dockerContext: ./php
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        value: <YOUR_BOT_TOKEN>

